# PostgreSQL Connection String (Supabase: Use the URI from Settings > Database)
# Pro-tip: Use the Transaction Mode pooler URI (usually port 6543) for best results.
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true
JWT_SECRET=your-secret-key
NODE_ENV=development
